"use client";

// HeroUI 3 does not require a global provider.
// This file is kept as a placeholder for future needs (e.g. ToastProvider, I18nProvider).
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
